import React from 'react';
import ReactDOM from 'react-dom';
import SpotifyProfile from './App';

ReactDOM.render(
  <React.StrictMode>
    <SpotifyProfile />
  </React.StrictMode>,
  document.getElementById('root'),
);
